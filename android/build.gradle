buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 23
        compileSdkVersion = 35
        targetSdkVersion = 34
        kotlinVersion = '1.9.25'
    }
    repositories {
        google()
        mavenCentral()
        // यह नया तरीका पक्का करेगा कि ग्रैडल आपके फोल्डर को सही से पहचाने
        maven { 
            url new File(projectDir, "../node_modules/expo-modules-gradle-plugin/android") 
        }
    }
    dependencies {
        classpath('com.android.tools.build:gradle')
        classpath('com.facebook.react:react-native-gradle-plugin')
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion")
        // स्थानीय फोल्डर से प्लगइन उठाना
        classpath "expo.modules:expo-modules-gradle-plugin:+"
    }
}
